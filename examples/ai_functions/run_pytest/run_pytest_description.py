run_pytest_description = {
    "name": "run_pytest",
    "description": "Run pytest and capture the output to a returned object. Returns 'all tests passed' if all tests pass, and more detailed output if they don't.",
    "parameters": {
        "type": "object",
        "properties": {}
    }
}